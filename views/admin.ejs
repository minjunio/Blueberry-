<!DOCTYPE html>
<html lang="en">
<head>
    <title>Admin - ExamHub</title>
    <%- include('partials/head') %>
</head>
<body class="p-4 md:p-8">
    <div class="max-w-6xl mx-auto relative z-10 mt-6">
        <div class="flex flex-col md:flex-row justify-between items-center mb-8 gap-4">
            <h1 class="text-3xl md:text-4xl font-black text-anime-textMain">ExamHub Admin 🛡️</h1>
            <a href="/" class="bg-white px-5 py-2 rounded-full font-black shadow-sm text-sm hover:bg-anime-pink transition">View Store</a>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-6 md:gap-8">
            
            <div class="glass-card p-6 lg:col-span-2">
                <h2 class="text-xl md:text-2xl font-black mb-4 border-b-2 border-dashed border-white pb-2">Manage Categories</h2>
                
                <form action="/admin/add-category" method="POST" class="flex gap-2 mb-6">
                    <input type="text" name="category_name" placeholder="New Category (e.g., ACT)" required class="flex-grow bg-white border-2 border-gray-100 rounded-xl px-4 py-2 font-bold outline-none focus:border-anime-accent text-sm">
                    <button type="submit" class="bg-anime-accent text-white font-black px-4 rounded-xl text-sm">Add</button>
                </form>

                <div class="grid grid-cols-2 md:grid-cols-3 gap-3">
                    <% categories.forEach(cat => { %>
                        <a href="/dashboard?category=<%= encodeURIComponent(cat.name) %>" class="bg-white/80 hover:bg-anime-skyLight border-2 border-transparent hover:border-anime-accent p-4 rounded-xl text-center group transition cursor-pointer">
                            <h3 class="font-black text-anime-textMain text-sm md:text-base"><%= cat.name %></h3>
                            <p class="text-[10px] text-anime-accent mt-1 opacity-0 group-hover:opacity-100 font-bold transition">+ Make Post</p>
                        </a>
                    <% }) %>
                </div>
            </div>

            <div class="space-y-6">
                <div class="glass-card p-6">
                    <h2 class="text-lg font-black mb-4">Create Vendor</h2>
                    <form action="/admin/create-user" method="POST" class="space-y-3">
                        <input type="text" name="username" placeholder="Username" required class="w-full bg-white border-2 border-gray-100 rounded-xl px-4 py-2 font-bold outline-none text-sm">
                        <input type="password" name="password" placeholder="Password" required class="w-full bg-white border-2 border-gray-100 rounded-xl px-4 py-2 font-bold outline-none text-sm">
                        <button type="submit" class="w-full bg-anime-textMain text-white font-black py-2 rounded-xl text-sm">Create</button>
                    </form>
                </div>

                <div class="glass-card p-6">
                    <h2 class="text-lg font-black mb-3">Active Vendors</h2>
                    <ul class="space-y-2 max-h-48 overflow-y-auto">
                        <% users.forEach(u => { %>
                            <li class="bg-white p-2 rounded-xl font-bold flex justify-between text-xs">
                                <span>@<%= u.username %></span>
                            </li>
                        <% }) %>
                    </ul>
                </div>
            </div>

        </div>
    </div>
</body>
</html>
